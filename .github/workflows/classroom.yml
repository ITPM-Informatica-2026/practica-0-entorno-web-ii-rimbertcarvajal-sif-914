name: Autograding Tests
'on':
- push
- repository_dispatch
permissions:
  checks: write
  actions: read
  contents: read
jobs:
  run-autograding-tests:
    runs-on: ubuntu-latest
    if: github.actor != 'github-classroom[bot]'
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    - name: Verificación de archivos
      id: verificacion-de-archivos
      uses: classroom-resources/autograding-command-grader@v1
      with:
        test-name: Verificación de archivos
        setup-command: ''
        command: ls index.html
        timeout: 1
        max-score: 100
    - name: Autograding Reporter
      uses: classroom-resources/autograding-grading-reporter@v1
      env:
        VERIFICACION-DE-ARCHIVOS_RESULTS: "${{steps.verificacion-de-archivos.outputs.result}}"
      with:
        runners: verificacion-de-archivos
